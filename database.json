{
  "defaultEnv": "defaultEnv",
  "defaultEnv": {
    "ENV": "NODE_ENV"
  },
  "development": {
    "driver": "pg",
    "url": {"ENV": "DATABASE_URL"}
  },
  "production": {
    "driver": "pg",
    "url": {"ENV": "DATABASE_URL"}
  }
}